import 'package:flutter/material.dart';

class TaskListMonthWidget extends StatelessWidget {
  const TaskListMonthWidget({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder(

    );
  }
}
